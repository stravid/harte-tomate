# Setup & Development
Use `php -S localhost:4000 backend.php` to start a local backend server.

curl -X POST -v http://localhost:5555/pomodori --data "token=1234"
curl -X POST -v http://localhost:5555/pomodori/19 --data "token=1234&event=ended"
